# Security Policy

请不要在公开 Issue 中提交 Token、Wi‑Fi 密码、BLE 抓包或带个人信息的日志。

发现认证绕过、任意命令执行、OTA 校验绕过或敏感信息泄露时，请先通过维护者的
私下联系方式报告，并提供复现步骤、受影响版本和最小日志片段。

后台默认仅监听本机回环地址。若确需让屏幕通过 Wi-Fi 访问，只能显式配置私有局域网
地址；程序会拒绝公网监听地址、公网访问来源和公网后端地址。禁止通过端口转发、反向
代理或隧道把控制端口暴露到公网。
