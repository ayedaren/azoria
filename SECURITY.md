# Security Policy

请不要在公开 Issue 中提交 Token、Wi‑Fi 密码、BLE 抓包或带个人信息的日志。

发现认证绕过、任意命令执行、OTA 校验绕过或敏感信息泄露时，请先通过维护者的
私下联系方式报告，并提供复现步骤、受影响版本和最小日志片段。

Desktop 的 TCP 控制服务只监听本机私有 IPv4 接口。UDP 协调服务为接收子网广播而监听
通配地址，但会根据数据包来源选择匹配的私有接口，并拒绝非私有来源。程序允许主动访问
公网 HTTP/HTTPS，但不提供公网入站控制入口。禁止通过端口转发、反向代理或隧道把 TCP
`8732`、UDP `8733` 或 UDP `8734` 暴露到公网。

局域网控制协议默认不认证同一私有子网内的设备，因此应只在可信网络中运行。Desktop
使用心跳和 DDC/CI 可达性选择一个粘性 Master；此机制用于避免多个 Desktop 重复执行，
不构成对恶意局域网设备的安全隔离。
